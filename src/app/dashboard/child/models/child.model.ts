/**
 * Represents information about a child.
 */
export interface Child {
  name: string;          // The name of the child.
  dateOfBirth: string;  // The date of birth of the child (in string format).
}
